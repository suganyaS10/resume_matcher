# Secrets
.env

# Private data — résumé has PII, never commit
resume.pdf
resume.txt
*.pdf

# LLM-parsed résumé bullets — contains real experience (PII), never commit
resume_bullets.json

# Python
venv/
__pycache__/
*.py[cod]

# macOS
.DS_Store
